# DIJIANG 1.1 installation

Installed `1.23.0-DIJIANG.1.1` from the current working tree. This release includes
the Windows native performance reader and process shutdown/registry fixes.

The personal revision now uses `feature.fix`. Features and refactors increment
the first level and reset the second to zero. Optimizations and fixes increment
the second level. Historical `DIJIANG.1` is the baseline for `DIJIANG.1.0`.
Community workspace versions remain `1.23.0`.

- Six personal version/update-policy tests passed, including historical identity
  parsing, two-level revisions and malformed-version rejection.
- Web resources, the Electron main bundle and bundled OpenCode 1.18.30 verification
  passed. Existing native PTY modules passed under Electron 43.3.0. No native
  dependency was changed or rebuilt against the unavailable Spectre toolchain.
- Both the staged app and actual installed app returned `1.23.0-DIJIANG.1.1`
  through the desktop bridge, mounted the performance control and returned HTTP
  200 from `/api/system/performance` using isolated test data. The installed
  sample contained four related processes. This test used an unavailable external
  OpenCode endpoint and does not validate a model conversation.
- NSIS exited 0. The installed executable reports `1.23.0-DIJIANG.1.1`.
- The installer is 173737741 bytes, at
  `C:/Users/74756/Documents/OpenChamber-builds/1.23.0-DIJIANG.1.1/OpenChamber-1.23.0-DIJIANG.1.1-win-x64.exe`.
- The previous installation is preserved in `previous-install` beside that
  installer. User configuration and session data were retained.
- H: had about 210 MB free and NSIS could not write its output there. Packaging
  completed on C: from the verified staged app. Installation also used C: for
  temporary files. Its first attempt was stopped after waiting without replacing
  the old files; the retry completed successfully.

Installed test output and screenshot are in
`Temp/installed-dijiang-1.1-check/result.json` and `window.png`. Packaging output is
in `package.log` beside the installer. The standard is maintained in
[BUILD.md](../BUILD.md).
