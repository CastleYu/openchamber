# 2026-09-22 focused test command failures

This record covers the DIJIANG 3.7 baseline and focused reruns. It records
environment and fixture evidence without treating the full baseline as green.

## Baseline

- `node scripts/run-isolated-tests.mjs scripts`: 7/8 files passed. `scripts/bump-version.test.mjs` failed because the temporary fixture packed workspace dependency `1.23.1` after the bump expected `1.24.0`.
- `bun run --cwd packages/sdk test`: 13/14 files passed. `packages/sdk/scripts/examples.test.ts` reported checked-in bundle hashes different from bundles generated by the current SDK.
- `bun run --cwd packages/vscode test`: 43/46 files passed. The three reported failures were `bridge-git-process-runtime.test.ts`, `managed-opencode-process.test.ts`, and `gitPathDiff.test.ts`.
- `bun run --cwd packages/web test`: the elevated Vitest run remained active after the first 90 seconds. Its log contains failures in settings-helper, lifecycle-process, long-path, SSH install, OpenChamber routes, walkthrough, auth-store, worktree readiness, and several zero-test/error suites. The process session was left for the main agent to own.

The first non-elevated attempt for all four commands failed before test
execution with Windows `spawn EPERM`. The commands were retried with the same
arguments under the scoped elevated runner. Logs are in `.codex-temp/37-*-baseline.log`.

## Focused reruns

The three VS Code failures reproduced independently under the same Windows
environment and all passed:

- `bun test src/bridge-git-process-runtime.test.ts`: 3 passed, 1 platform skip.
- `bun test src/managed-opencode-process.test.ts`: 4 passed.
- `bun test src/gitPathDiff.test.ts`: 4 passed.

This indicates the baseline failures were affected by concurrent load or
subprocess contention. No assertions were removed and no platform test was
converted into a skip.

The SDK example bundles were regenerated with the installed Bun and the focused
suite then passed: 3 tests, 24 assertions. The attempted `npx --yes
bun@1.4.2 --version` could not use the local cache and did not download a new
tool (`ENOTCACHED`); no global install or configuration change was made.

The bump-version test still needs the repository's intended Bun 1.4.2 behavior
or a release-script fix that preserves frozen-lockfile packing. It was not
silently changed to accept the stale dependency version.
