# DIJIANG 3.7 上游集成 — 2026-09-24

## Git 范围

- 执行及发布分支：`codex/personal`。
- 个人分支起点：`a81e9c6658f41962298644d07fa40f33566a674a`。
- 用户原有修改：五处已跟踪更改和四份未跟踪维护文档，已保存在储藏记录 `84e0c878f9532ccc829f27c671e65c64caf50634` 中，之后恢复供审查。初始索引为空。
- 首次合并社区上游：`83ec4fbde25a9d141785716bebe0371925f895b5`，已集成到 `4ac81115c17c203c89c5b52f93a930af32ea2163`。
- 最终于 2026-09-24 检查远端时，社区最新 `main` 为：
  `0af1eb00cacee54492295935549c513af614a5ac`。
- 已验证的集成合并：`872de3db6`（个人分支起点和社区最新提交均为其祖先）。
- 合并后的双语历史端点修正：`8d80b1bde`；六项针对性测试通过，共 2,269 条断言。
- 恢复引用：`codex/backup-dijiang-3.7-20260922`。暂存内容和引用仍可用；未执行强制推送或无关清理。

## 冲突处理决定

- 保留 DIJIANG 原生媒体/ZIP 打开方式、PlantUML 模式、个人更新策略和双语历史，同时采用上游的制品预览、OpenCode 2.x 合约及新增设置分区。
- 将受管 MCP 注入改为 OpenCode 2.x 插件目录，并将配置、会话和 MCP 调用改为当前客户端形式。配置读取失败时，保留已释放至空闲状态的连接以便重试。
- 在新的上游事件管线中保留前台和后台帧预算。包含 60 个增量的确定性测试检查后台刷新是否无损。
- 采用上游的 `changelog/unreleased.md`，并重新生成锁文件；未编辑更新日志正文，内容仍由维护者编写。
- 修复 Windows 测试夹具和运行器假设。在 Windows 上跳过仅支持 POSIX 的安装脚本；便携版构建会单独验证 Windows 二进制文件。

## 验证

- `bun install --frozen-lockfile`：通过，使用 Bun 1.4.2。
- `bun run type-check`：所有工作区均通过。
- `bun run lint`：通过；仍有一条既存的 React hook 警告。
- `bun run test`：scripts 9/9 个文件；SDK 15/15；UI 583/583；VS Code 50/50；Electron 33/33；Web 有 262 个通过、7 个跳过的文件，以及 3,768 个通过、141 个跳过的测试。根命令退出码为 0。
- `bun run changelog:check`：通过。合并提交前 `git diff --cached --check` 通过。冲突路径列表为空。
- `bun run dead-code`：已完成；非阻断报告中有既存积压，作者编写的 MCP/事件管线路径没有发现问题。
- 指定范围的 `bunx oxlint` 在大型上游 UI 文件中报告既存 anti-slop 问题，另有一处既存 VS Code 测试断言问题；未屏蔽任何规则。

## 尚未完成的验收范围

完整测试运行验证了各测试运行器中的可执行合约，但没有验证正在运行的外部 MCP 服务、VS Code 界面或移动设备。隔离的 Windows 打包应用已启动，使用受管 OpenCode 2.0.15，显示主界面和双语筛选历史，且退出正常；退出后没有受管注册表条目。本地便携版包装器尚未完成完整生命周期检查：一次运行进入 Electron 渲染进程后，CDP 导航中断了探测；另一次运行因本地磁盘空间接近耗尽，在解压前退出。本地 VS2022 Spectre 解决方法只影响被忽略的 `node_modules`，且已恢复。已提交的 CI 源码保留依赖项原有设置。随后远端 [Release v1.24.2-DIJIANG.3.7](https://github.com/CastleYu/openchamber/releases/tag/v1.24.2-DIJIANG.3.7) 已发布并核验五个资源；完整发布记录见 [DIJIANG-3.7-DELIVERY.md](../DIJIANG-3.7-DELIVERY.md)。
