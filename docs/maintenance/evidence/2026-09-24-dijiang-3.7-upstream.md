# DIJIANG 3.7 upstream integration — 2026-09-24

## Git scope

- Invoking and publication branch: `codex/personal`.
- Personal starting point: `a81e9c6658f41962298644d07fa40f33566a674a`.
- Original user edits: five tracked changes and four untracked maintenance
  documents, preserved in stash `84e0c878f9532ccc829f27c671e65c64caf50634`
  and restored for review. The initial index was empty.
- First community intake: `83ec4fbde25a9d141785716bebe0371925f895b5`,
  integrated in `4ac81115c17c203c89c5b52f93a930af32ea2163`.
- Latest community `main` at the final 2026-09-24 remote check:
  `0af1eb00cacee54492295935549c513af614a5ac`.
- Validated integration merge: `872de3db6` (both the personal starting point
  and latest community tip are ancestors).
- Bilingual history endpoint correction after the merge: `8d80b1bde`; its
  six focused tests passed with 2,269 assertions.
- Recovery ref: `codex/backup-dijiang-3.7-20260922`. The stash and ref remain
  available; no force push or unrelated cleanup was performed.

## Conflict decisions

- Kept the DIJIANG native media/ZIP opening, PlantUML mode, personal update
  policy and bilingual history while adopting upstream artifact preview,
  OpenCode 2.x contracts and new Settings sections.
- Converted managed MCP injection to an OpenCode 2.x plugin directory and
  changed config, session and MCP calls to the current client shape. Failed
  config reads retain idle-released connections for retry.
- Retained focused and background frame budgets in the new upstream event
  pipeline. A deterministic 60-delta test checks that background flushing is
  lossless.
- Took upstream's `changelog/unreleased.md` and regenerated lockfile without
  editing the changelog body; its authoring remains maintainer-owned.
- Repaired Windows test fixtures and runner assumptions. POSIX-only installer
  scripts are skipped on Windows; the portable build verifies the Windows
  binary separately.

## Validation

- `bun install --frozen-lockfile`: passed with Bun 1.4.2.
- `bun run type-check`: passed in every workspace.
- `bun run lint`: passed; one existing React hook warning remains.
- `bun run test`: scripts 9/9 files; SDK 15/15; UI 583/583; VS Code 50/50;
  Electron 33/33; Web 262 passed/7 skipped files and 3,768 passed/141 skipped
  tests. Root exit code 0.
- `bun run changelog:check`: passed. `git diff --cached --check` passed before
  merge commit. Conflict path list was empty.
- `bun run dead-code`: completed; the nonblocking report has existing backlog
  and no finding in the authored MCP/event-pipeline paths.
- Scoped `bunx oxlint` reported existing anti-slop findings in large upstream
  UI files plus one pre-existing VS Code test assertion; no rule was silenced.

## Remaining acceptance boundary

The full test run proves the executable contracts in its runners, not a live
external MCP service, VS Code UI, or mobile device. An isolated packaged
Windows app did launch, use managed OpenCode 2.0.15, render its main UI and
bilingual filtered history, and exit cleanly with no managed registry entry.
The local portable wrapper did not complete a full lifecycle check: one run
reached an Electron renderer before CDP navigation interrupted the probe; a
second exited before extraction when the local disk was nearly full. The local
VS2022 Spectre workaround affected only ignored `node_modules` and was
restored. The committed CI source keeps the dependency's original setting.
Remote Release checks remain outstanding and are tracked in
`docs/maintenance/DIJIANG-3.7-DELIVERY.md`.
